# Assets

```text
assets/
├── animation_templates/   可重複套用的人形動作模板
├── banners/               廣告 Banner、網頁預覽與 Spine 3.8 runtime
└── characters/            遊戲角色 Spine 專案與 runtime
```

每個正式素材都有自己的 `README.md`。一般查看效果只需要進入 Banner
的 `gallery/`，或角色的 `runtime/`；`source/` 與 `tools/` 是重新建置時
才會使用的內容。

